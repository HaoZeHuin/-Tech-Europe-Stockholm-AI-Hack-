# ===================================================================
# MASTER REQUIREMENTS - ALL SERVICES UNIFIED
# TESTED WORKING VERSIONS - No conflicts guaranteed
# ===================================================================

# CRITICAL: OpenAI packages (exact versions to avoid conflicts)
openai==1.107.2
openai-agents==0.3.0

# CRITICAL: WebSocket support (exact version for compatibility)
websockets==14.2

# Core FastAPI stack (compatible versions)
fastapi>=0.104.1
uvicorn>=0.35.0
starlette>=0.27.0

# HTTP and async (let pip resolve compatible versions)
httpx>=0.28.1
anyio>=3.7.1,<5.0.0
sniffio>=1.3.0

# Data validation and parsing
pydantic>=2.11.9
pydantic-settings>=2.7.1

# Environment and configuration
python-dotenv>=1.0.0
python-multipart>=0.0.20

# Logging
python-json-logger>=2.0.7

# Redis for caching and sessions (N8N service)
redis>=5.0.1

# Vector database client (RAG service)
weaviate-client>=3.25.3

# Additional utilities
aiofiles>=23.0.0

# Development and testing (optional)
pytest>=7.4.4

# ===================================================================
# IMPORTANT NOTES:
# 1. openai-agents==0.3.0 requires: openai<2,>=1.107.1
# 2. We pin openai==1.107.2 which satisfies this constraint  
# 3. websockets==14.2 is tested and compatible with all services
# 4. Other packages use compatible ranges to avoid conflicts
# 5. This file replaces ALL other requirements.txt files
# ===================================================================
